{
	"name": "agentos",
	"version": "0.1.0",
	"description": "Agent Operating System - makes agents proactive",
	"type": "module",
	"bin": {
		"agentos": "./dist/index.js"
	},
	"scripts": {
		"build": "bun build src/index.ts --outdir dist --minify --target bun",
		"start": "bun run src/index.ts",
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit -p tsconfig.typecheck.json"
	},
	"dependencies": {
		"@ai-sdk/openai-compatible": "^1.0.0",
		"ai": "^6.0.86",
		"discord.js": "^14.14.0",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
