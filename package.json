{
  "name": "agentos",
  "version": "0.1.0",
  "description": "Agent Operating System - makes agents proactive",
  "type": "module",
  "bin": {
    "agentos": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --minify",
    "start": "bun run src/cli.ts",
    "test": "bun test"
  },
  "dependencies": {
    "ai": "^4.0.0",
    "@ai-sdk/openai-compatible": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
